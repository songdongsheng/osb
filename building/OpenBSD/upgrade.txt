export PKG_PATH=http://ftp.openbsd.org/pub/OpenBSD/snapshots/packages/`machine -a`/

http://ftp.openbsd.org/pub/OpenBSD/snapshots/ports.tar.gz

/sbin/vnconfig  -l

umount /dev/vnd0c
/sbin/vnconfig -u vnd0

/sbin/vnconfig vnd0 /root/OpenBSD-snapshots-amd64-20151012165259-install58.iso
/sbin/mount -t cd9660 /dev/vnd0c /root/iso

cd /root/iso/5.8/amd64/

/bin/ln -f  /bsd        /obsd
/bin/cp     bsd.rd      /bsd.rd
/bin/cp     bsd         /bsd.sp
/bin/cp     bsd.mp      /bsd.new
/bin/mv     /bsd.new    /bsd

cp /sbin/reboot /root/reboot
ldd /root/reboot
file /root/reboot

tar -C / -xzpf man58.tgz
tar -C / -xzpf xshare58.tgz
tar -C / -xzpf xbase58.tgz
tar -C / -xzpf comp58.tgz
tar -C / -xzpf base58.tgz

/root/reboot

cd /dev
./MAKEDEV all

/usr/sbin/sysmerge

cd /usr/src
cvs -q -d anoncvs@anoncvs.au.openbsd.org:/cvs up -Pd

cd /usr/ports
cvs -q -d anoncvs@anoncvs.au.openbsd.org:/cvs up -Pd
